apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-demo-green
  namespace: java-demo
  labels:
    app: java-demo-app
    version: green
  annotations:
    kubernetes.io/change-cause: "Green candidate environment running v0.0.3"
spec:
  replicas: 2
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: java-demo-app
      version: green
  template:
    metadata:
      labels:
        app: java-demo-app
        version: green
    spec:
      containers:
      - name: java-demo
        image: jpalaparthi/bmw-k8s-java-demo:v0.0.3
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_DATASOURCE_URL
          valueFrom:
            configMapKeyRef:
              name: postgres-config
              key: SPRING_DATASOURCE_URL
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            configMapKeyRef:
              name: postgres-config
              key: SPRING_DATASOURCE_USERNAME
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: POSTGRES_PASSWORD
