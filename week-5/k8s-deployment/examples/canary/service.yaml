apiVersion: v1
kind: Service
metadata:
  name: java-demo-canary-service
  namespace: java-demo
spec:
  type: LoadBalancer
  # Intentionally does not select 'track'. Both stable and canary Pods receive traffic.
  selector:
    app: java-demo-canary
  ports:
  - port: 8080
    targetPort: 8080
